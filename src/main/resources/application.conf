# Akka settings
akka.loglevel = INFO

# Spray-can settings
spray.can.server {
  request-timeout = 160s
  idle-timeout = 220s
}

# Default config path (may be local or cluster)
# May be overrided by first command line parameter
config.path = "cluster"